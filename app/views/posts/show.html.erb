<h1>投稿詳細</h1>
<h2><%= @post.title %></h2>
<p><%= @post.body %></p>
<%= link_to "編集", edit_post_path(@post.id) %>
<%= link_to "削除", post_path(@post.id), method: :delete, "data-confirm" => "本当に削除しますか？" %>